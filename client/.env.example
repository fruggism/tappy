# Lasciala vuota: in produzione client e API stanno sullo stesso dominio.
# In locale con "netlify dev" (dalla root del repo) non serve impostarla:
# netlify dev serve sia il client che le funzioni sulla stessa porta (8888).
VITE_API_URL=
