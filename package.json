{
  "name": "tappy-functions",
  "private": true,
  "version": "1.0.0",
  "description": "Dipendenze delle Netlify Functions di tappy (backend su Airtable)",
  "scripts": {
    "dev": "npx -y netlify-cli dev",
    "test": "node tests/frupass.test.mjs && node tests/auth-routes.test.mjs && node tests/check-airtable.test.mjs",
    "setup-env": "node scripts/setup-env.js",
    "doctor": "node scripts/doctor.js",
    "check-airtable": "node scripts/check-airtable.js"
  },
  "dependencies": {
    "airtable": "^0.12.2",
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "serverless-http": "^3.2.0"
  }
}
